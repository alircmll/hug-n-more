<div id="heroHeaderHome" class="jumbotron jumbotron-fluid h-100 vh-70 d-flex img-wrapper">
  <div  class="container flex-column text-center my-auto">
    <h1 class="display-4 ">Bienvenue sur Hug'N'More</h1>
    <p class="lead">Prends ton temps... fais un câlin ♥️</p>
  </div>
</div>
<!-- <div id="searchBar">
  <div class="container-fluid align-items-center">
    <div id="searchBarContent" class="col-12 col-md-10 offset-md-1 text-center">
      <%#= form_tag hugs_path, method: :get do %>
        <%#= text_field_tag :s,
              params[:s],
              class: "form-control, align-self-center",
              placeholder: "Hug ? Where ?",
              type: "search"
            %>
      <%# end %>
    </div>
  </div>
</div> -->
<% #if @hugs.select(&:friendly?).count > 0 %>
  <section id="friendly" class="container-fluid">
    <div  class="row text-center">
      <div class="col-12 mb-4">
        <h2 class="text-primary display-4 text-uppercase"><strong>Friendly</strong></h2>
      </div>
      <% @hugs.select(&:friendly?).each do |hug| %>
        <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4">
          <div class="wrapper">
            <div class="card"> <%= image_tag 'friendly.jpg', class: "card-img" %>
              <div class="info">
                <h3><%= hug.title %></h3>
                <p><%= hug.description.first(70) %>...</p>
                <button><%= link_to "Details", hug_path(hug) %></button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<%# end %>
<%# if @hugs.select(&:tiny?).count > 0 %>
  <section id="tiny" class="container-fluid">
    <div  class="row text-center">
      <div class="col-12">
        <h2 class="text-primary display-4 text-uppercase">Tiny</h2>
      </div>
      <% @hugs.select(&:tiny?).each do |hug| %>
        <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4">
          <div class="wrapper">
            <div class="card"><%= image_tag 'tiny.jpg', class: "card-img" %>
              <div class="info">
                <h3><%= hug.title %></h3>
                <p><%= hug.description.first(70) %>...</p>
                <button><%= link_to "Details", hug_path(hug) %></button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<%# end %>
<%# if @hugs.select(&:fast?).count > 0 %>
  <section id="fast" class="container-fluid">
    <div  class="row text-center">
      <div class="col-12 mb-4">
        <h2 class="text-primary display-4 text-uppercase">Fast</h2>
      </div>
      <% @hugs.select(&:fast?).each do |hug| %>
        <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4" >
          <div class="wrapper">
            <div class="card"><%= image_tag 'fast.jpg', class: "card-img" %>
              <div class="info">
                <h3><%= hug.title %></h3>
                <p><%= hug.description.first(70) %>...</p>
                <button><%= link_to "Details", hug_path(hug) %></button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<%# end %>
<%# if @hugs.select(&:more?).count > 0 %>
  <section id="more" class="container-fluid">
    <div  class="row text-center">
      <div class="col-12 mb-4">
        <h2 class="text-primary display-4 text-uppercase">More</h2>
      </div>
      <% @hugs.select(&:more?).each do |hug| %>
        <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4">
          <div class="wrapper">
            <div class="card"><%= image_tag 'huggers.jpg', class: "card-img" %>
              <div class="info">
                <h3><%= hug.title %></h3>
                <p><%= hug.description.first(70) %>...</p>
                <button><%= link_to "Details", hug_path(hug) %></button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<%# end %>
<section class="bg-white">
  <div class="container">
    <header class="section-header">
      <small>Rejoins-nous maintenant</small>
      <h2 class="lead-8"><strong>Boost Your ♥️</strong></h2>
      <hr>
      <p class="lead-2">Inscrit-toi gratuitement et fait parti des milliers de personnes qui se sont trouvées grâce à Hug'n'more.</p>
      <p class="text-center">
        <%= link_to 'Faire un câlin maintenant !', new_user_registration_path, class: "btn btn-lg btn-primary w-250 my-2 text-white" %>
        <br>

        <small class="text-lighter">Déjà inscrit? </small>
          <%= link_to "S'identifier", new_user_session_path%>
        </p>
      </header>
    </div>
  </section>

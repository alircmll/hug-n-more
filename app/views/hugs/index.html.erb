<div id="heroHeaderHome" class="jumbotron jumbotron-fluid h-100 vh-70 d-flex img-wrapper">
  <div  class="container flex-column text-center my-auto">
    <h1 class="display-4 ">Welcome to Hug'N'More</h1>
    <p class="lead">Take your time... make a hug ♥️</p>
  </div>
</div>
<div id="searchBar">
  <div class="container-fluid align-items-center">
    <div id="searchBarContent" class="col-12 col-md-10 offset-md-1 text-center">
      <%= form_tag hugs_path, method: :get do %>
        <%= text_field_tag :s,
              params[:s],
              class: "form-control, align-self-center",
              placeholder: "Hug ? Where ?",
              type: "search"
            %>
        <%# submit_tag "Search", class: "btn btn-primary" %>
      <% end %>
    </div>
  </div>
</div>
<section id="friendly" class="container-fluid">
  <div  class="row text-center">
    <div class="col-12 mb-4">
      <h2 class="text-primary display-2 text-uppercase"><strong>Friendly</strong></h2>
    </div>
    <% @hugs.select(&:friendly?).each do |hug| %>
      <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4">
        <div class="wrapper">
          <div class="card"><%= cl_image_tag hug.photo, class: "card-img" %>
            <div class="info">
              <h3><%= hug.title %></h3>
              <p><%= hug.description.first(70) %>...</p>
              <button><%= link_to "Details", hug_path(hug) %></button>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<% end %>
<%if @hugs.select(&:tiny?).count > 0 %>
  <section id="tiny" class="container-fluid">
    <div  class="row text-center">
      <div class="col-12">
        <h2 class="text-primary display-2 text-uppercase">Tiny</h2>
      </div>
      <% @hugs.select(&:tiny?).each do |hug| %>
        <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4">
          <div class="wrapper">
            <div class="card"><%= cl_image_tag hug.photo, class: "card-img" %>
              <div class="info">
                <h3><%= hug.title %></h3>
                <p><%= hug.description.first(70) %>...</p>
                <button><%= link_to "Details", hug_path(hug) %></button>
              </div>
            </div>
          </div>
        </div>
    </div>
    <% end %>
  </div>
</section>
<section id="fast" class="container-fluid">
  <div  class="row text-center">
    <div class="col-12 mb-4">
      <h2 class="text-primary display-2 text-uppercase">Fast</h2>
    </div>
  </section>
<% end %>
<%if @hugs.select(&:fast?).count > 0 %>
  <section id="fast" class="container-fluid">
    <div  class="row">
      <div class="col-12 mb-4">
        <h2 class="text-primary display-2 text-uppercase">Fast</h2>
      </div>
      <% @hugs.select(&:fast?).each do |hug| %>
        <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4" >
          <div class="wrapper">
            <div class="card"><%= cl_image_tag hug.photo, class: "card-img" %>
              <div class="info">
                <h3><%= hug.title %></h3>
                <p><%= hug.description.first(70) %>...</p>
                <button><%= link_to "Details", hug_path(hug) %></button>
              </div>
            </div>
          </div>
        </div>
    </div>
    <% end %>
  </div>
</section>
<section id="more" class="container-fluid">
  <div  class="row text-center">
    <div class="col-12 mb-4">
      <h2 class="text-primary display-2 text-uppercase">More</h2>
    </div>
  </section>
<% end %>
<%if @hugs.select(&:more?).count > 0 %>
  <section id="more" class="container-fluid">
    <div  class="row">
      <div class="col-12 mb-4">
        <h2 class="text-primary display-2 text-uppercase">More</h2>
      </div>
      <% @hugs.select(&:more?).each do |hug| %>
        <div class="col-12 col-sm-12 col-md-6 col-lg-4 col-xl-3 mb-4 mb-sm-4">
          <div class="wrapper">
            <div class="card"><%= cl_image_tag hug.photo, class: "card-img" %>
              <div class="info">
                <h3><%= hug.title %></h3>
                <p><%= hug.description.first(70) %>...</p>
                <button><%= link_to "Details", hug_path(hug) %></button>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </section>
<% end %>
<section class="bg-white">
  <div class="container">
    <header class="section-header">
      <small>Join Us Now</small>
      <h2 class="lead-8"><strong>Boost Your 🍑</strong></h2>
      <hr>
      <p class="lead-2">Sign up for free and become one of the millions of people around the world who have fallen in love with Hug'n'more.</p>
      <p class="text-center">
        <!-- <a class="btn btn-lg btn-primary w-250" href="#">Make a Hug Right Now!</a><br> -->
        <%= link_to 'Make a Hug Right Now!', new_user_registration_path, class: "btn btn-lg btn-primary w-250" %>
        <br>
        <small class="text-lighter">Already registered? 
          <!-- <a href="#">Login</a> -->
          <%= link_to 'Login', new_user_session_path%>
        </small>
      </p>
    </header>
  </div>
</section>
